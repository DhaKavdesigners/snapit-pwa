# SnapIt — Store Image Folder

Place your store logo image in this folder and name it exactly:
**`logo.jpg`**

The app will automatically pick it up from this path:
`/images/stores/mhetha-stores/logo.jpg`

If the image is missing, the app will gracefully fall back to a stock photo so the UI never looks broken.
